Haskell application:
    evaluate circuits 
        generate random tests
        compare to cryptol (can i run cryptol inside hs? probably)

    translate between formats
        verilog parser/writer
        acirc parser/writer

    provide information
        depth
        degree
        other stuff

    convert scripting stuff from bash
        call out to abc, yosys
